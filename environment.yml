name: pdf-query-bot
channels:
  - conda-forge
  - defaults

dependencies:
  - ipykernel

  - pip:
      - langchain
      - openai
      - PyPDF2
      - faiss-cpu
      - langchain-community
      - tiktoken